use crate::deadline_clock::{DeadlineClock, Reset};
use crate::peer;

/*
pub struct Heartbeater {
    peer: peer::GRPCPeer,
    reset: Reset,
}

impl Heartbeater {
    pub fn new(peer: peer::GRPCPeer) -> Self {
        let dc, reset
        Self { peer }
    }

    fn run(&self) {
        loop {
            // do

        }
    }
}
*/
